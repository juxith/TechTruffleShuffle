@model TechTruffleShuffle.UI.Models.BlogPostViewModel
@{
    ViewBag.Title = "CreateBlog";
}

<head>

</head>

<div class="container">
    <div>
        @using (Html.BeginForm("CreateBlog", "Home"))
        {
            @Html.HiddenFor(m => m.User.Id)
            <div class="col-xs-12 col-sm-7">
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h4 class="panel-title">Create Blog</h4>
                    </div>
                    <div class="panel-body">
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Title, new { @class = "form-control", @placeholder="Enter Title Here" })
                            @Html.ValidationMessageFor(c => c.Title, null, new { @class = "error" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.EventDate, new { @class = "form-control", @placeholder = "Event Date (MM/DD/YYYY)" })
                            @Html.ValidationMessageFor(c => c.EventDate, null, new { @class = "error" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Hashtags, new { @class = "form-control", @placeholder = "#Hashtags"})
                            @Html.ValidationMessageFor(c => c.Hashtags, null, new { @class = "error" })
                        </div>
                        <div class="form-group">
                            @Html.TextAreaFor(m => m.BlogContent, new { @class = "form-control", @id = "blogtextarea"})
                            @Html.ValidationMessageFor(c => c.BlogContent, null, new { @class = "error" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.BlogCategory, new { @class = "form-control", @placeholder = "Needs to be drop down at some point"})
                            @Html.ValidationMessageFor(c => c.EventDate, null, new { @class = "error" })
                        </div>
                        <div class="form-group">
                            <label>Featured</label>
                            @Html.RadioButtonFor(m => m.IsFeatured, "true")
                            @Html.ValidationMessageFor(c => c.EventDate, null, new { @class = "error" })
                        </div>
                        <input type="submit" name="submit" value="Save" class="btn btn-warning"/>
                        <input type="submit" name="submit" value="Post" class="btn btn-warning"/>
                        <input type="submit" name="submit" value="Publish" class="btn btn-danger" />
                    </div>
                </div>
            </div>
        }
    </div>
</div>

